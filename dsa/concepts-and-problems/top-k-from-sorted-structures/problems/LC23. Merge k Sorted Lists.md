# LC23. Merge k Sorted Lists

[LeetCode Problem Link](https://leetcode.com/problems/merge-k-sorted-lists/)

## Intuition & Approach
- We want to merge $K$ sorted linked lists into one sorted list.
- We can solve this using a Min-Heap (priority queue) containing the head nodes of all lists:
  - The min-heap stores elements as `(node_value, node_pointer)`.
  - Push the head of each of the $K$ lists into the min-heap.
  - While the heap is not empty, pop the minimum node. Append it to our merged list.
  - If the popped node has a next node (`node->next`), push `node->next` into the min-heap.
- This maintains a sorted list of active heads, merging them in $O(N \log K)$ time.

## Code Implementation
```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* mergeTwoLists(ListNode* list1, ListNode* list2) {
        if(!list1 && !list2) return NULL;
        if(list1 && !list2) return list1;
        if(list2 && !list1) return list2;
        ListNode* head=NULL;
        ListNode* start=NULL;
        ListNode* temp1=list1;
        ListNode* temp2=list2;
        if(list1->val <=list2->val){
            start=list1;
            temp1=temp1->next;
        }
        else{
            start=list2;
            temp2=temp2->next;

        }
        head=start;
        while(temp1 && temp2){
            if(temp1->val<temp2->val){
                start->next=temp1;
                temp1=temp1->next;

            }
            else{
                start->next=temp2;
                temp2=temp2->next;
            }
            start=start->next;
        }
        if(temp1){
            start->next=temp1;
        }
        if( temp2){
            start->next=temp2;
        }
        return head;
    }
    ListNode* mergeKLists(vector<ListNode*>& lists) {
        // ListNode* l1=lists[0];
        // if(lists==NULL ) return NULL;
        // int n=lists.size();
        // if(n==0) return NULL;
        // if(n==1) return lists[0];
        // ListNode* mergedNode=NULL;
        // for(int i=0;i<n;i++){
        //     mergedNode=mergeTwoLists(mergedNode,lists[i]);
        // }
        // return mergedNode;

        //Optimal
        priority_queue<pair<int,ListNode*>,vector<pair<int,ListNode*>>,greater<pair<int,ListNode*>>> pq;
        for(int i=0;i<lists.size();i++){
            if(lists[i])
            pq.push({lists[i]->val,lists[i]});
        }
        ListNode* dummy=new ListNode(-1);
        ListNode* temp = dummy;
        while(!pq.empty()){
            ListNode* node=pq.top().second;
            pq.pop();
            temp->next=node;
            if(node->next) pq.push({node->next->val,node->next});
            temp=temp->next;
            

        }
        return dummy->next;
    }
};
```

## Complexity Analysis
- **Time Complexity**: $O(N \log K)$ where $N$ is the total number of nodes and $K$ is the number of linked lists.
- **Space Complexity**: $O(K)$ to store at most $K$ nodes in the priority queue.

---
**Key Takeaways**:
- **K-Way Merge via Min-Heap**: Min-heaps track the minimum active heads in $K$-way merges, reducing comparison time to logarithmic $O(\log K)$.
